{
  "name": "researcher-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:tistory": "node scripts/fetch-tistory.mjs",
    "sync:news": "node scripts/fetch-news.mjs",
    "sync:content": "npm run sync:tistory && npm run sync:news",
    "predev": "npm run sync:content",
    "dev": "astro dev",
    "prebuild": "npm run sync:content",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.6.0",
    "astro": "^5.16.0"
  }
}
