---
interface Props {
  eyebrow: string;
  title: string;
  text?: string;
}
const { eyebrow, title, text } = Astro.props;
---
<section class="page-intro shell">
  <p class="eyebrow">{eyebrow}</p>
  <h1>{title}</h1>
  {text && <p class="page-lede">{text}</p>}
</section>
